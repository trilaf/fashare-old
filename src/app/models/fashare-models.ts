export interface Data {
    name: string,
    size: number,
    url: any,
    type: string
}

export interface fileList {
  fileList: any;
}

export interface shortChannelID {
  id: string;
}